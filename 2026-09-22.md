# 2026-09-22

## Spec 001「对话工作台」界面与交互细节补全（纯增量）

- 目标文件：`specs/001-conversation/spec 3.md`（工作副本，非 spec.md 正本；untracked）。
- 用户约束：**现有部分不能改动，只做增加**。第一轮改了 header（Created/Status/Feature Branch）并试图把 `## 附录：原 AC 编号对照` 改名为「附录 A」，被用户撤销并明确「只做增加」——教训：此类工作副本只允许在既有块之间**插入**新章节，绝不重写既有行。
- 落地方式：在 Key Entities 与 `## Success Criteria` 之间插入 `## 界面与交互细节（2026-09-22 追加 · 纯增量）`，A~M 十三节（约 170 行），既有 US/FR/SC/Edge Cases/Assumptions/Non-Goals/Open Questions 原文零改动。
- 标记约定：`[缺口]` 缺失细节、`[候选]` 优化备选、`[待确认]` 需裁定后才可转 FR；均声明尚未构成需求。
- 最值得补 FR 的 3 个真缺口：①流式中断后已下发内容的处置（FR-023/025/036 均未覆盖）；②Agent 调试台同页多轮结果呈现（FR-019/021 未定义）；③图片粘贴/拖拽录入入口（FR-009b 未定义）。
- 参考平台对照：ChatGPT、Claude、Gemini、Microsoft Copilot、Perplexity、Azure AI Foundry / OpenAI Playground / Vertex AI Studio、Postman / Insomnia、Dify / Coze。
- `specs/**` 属守门点，需人审后方可合并。

## 同文件二次加工：新增节按本文件原生排版重排（用户指令）

- 用户要求「新加的这一节按照文档本来的格式排版」。已按既有约定改造，可复用排版规则：子节标题 `### 中文短语（对齐 USx / FR-00x~FR-00y）` 不带 A./B. 字母编号；条目统一 `- **标签**: 内容`；不确定性写「（缺口）」「（候选）」「（待确认）」而非 `[缺口]`；UI 文案用「」而非反引号（反引号只给 `detail` 这类字段名）；前言用 `> 注：...` 单行引用 + `**标签**: 内容` 段落；表格分隔行用 `|-------|--------|` 纯横线；正文避免 `+`/`→`/`↔`，改中文「加」「到」。
- 结果：该节现为 346~522 行（13 小节 + 3 张表），既有内容仍零改动。
