# 2026-09-20

## Spec 001 对话工作台落地（marketplace - chat_agent.html）
- 按《GRC-AI平台-Spec001-对话工作台-功能清单-2026-09-17.md》实现 chat 全部 38 条 FR 的可交互原型。
- **用户明确要求：不要改变原本界面布局**。初版方案（顶部栏+建议卡片+移动 composer）被否决；最终方案保持原始双形态：欢迎屏 chat-box（chips: +/模型/Knowledge/Deep Analysis）+ 消息态底部 chat-bar（input+Send/Stop）。Agent/History 抽屉为 JS 动态创建的覆盖层，不改基础布局。
- 实现要点：
  - 会话存 localStorage `grc.chat.sessions.v1`（90 天清理、500 上限、软删、重命名后不被自动标题覆盖）；首跑 seed 3 条演示会话（含已下架/出站停用 → 只读 FR-032）。
  - 模型芯片下拉只列对话/多模态模型（FR-005）；vision 判定不在 SDK，非 vision 模型发图 → 模拟 Provider 400（FR-009e）。
  - 图片：JPEG/PNG/WebP、≤4、10MiB/张、20MiB 总量、SVG/GIF 拒绝（FR-009b）。
  - KB 挂载面板：数据镜像 knowledge.html 的 8 个域（可用+Consumer+，Building 库不出现），目录树/搜索/多选库/单选文档、上限 5、超限 toast 用 spec 原文（FR-010~012）；检索归并 top5→top10 按切片去重（FR-013）；引用条+展开面板（位次/相似度/库/文档/打开原文→modal→knowledge.html）（FR-014）；SVG 图表证据内嵌（FR-015）。
  - 流式打字机（110ms 节流重渲染、不强制滚动跟随、长文折叠 FR-027c）；自研 mini Markdown（表格横滑/代码高亮+行号+复制/mermaid 节点流/数学）（FR-027b）。
  - 护栏演示触发词：输入侧 身份证|护照|银行卡|炸弹|忽略指令 → 拦截拒答；输出侧 "撤回演示/输出护栏" → 55% 处瞬时撤回零字留存（FR-023/024）；"失败演示/[fail]" → 错误+重试不落库（FR-036）；停止生成保留已下发前缀（FR-025）；生成中禁发送+提示，Send 原位变 Stop（FR-026）。
  - Agent 透传：详情页订阅 Agent 有 "Use in Chat"（FR-017）；Policy Retrieval Assistant 非流式 buffered 全量呈现（FR-022）；新建会话带 sys-note「不带入此前对话历史」（FR-019）；Chat 与资产会话切换 chips 随 mode-agent 隐藏（FR-005/006/010）。
  - 我的资产抽屉：已订阅/收藏两区+Recent/Name 排序；已下架置灰、出站停用/服务不可用 toast 三类文案（FR-033~035）；MCP/未订阅进详情页。
  - ASSETS 演示标记：AML Screening/Fraud Detection=subscribed，Fraud Detection=serviceDown，Regulatory Alert Monitor=delisted，Transaction Pattern Analyzer=outboundDisabled，fav：Compliance Q&A/Policy Retrieval/Confluence MCP。
- 中途限制（保持布局的取舍）：功能 chips 只在欢迎屏可用，会话中途不可改模型/挂载/深度开关；Memory/Read rail 提示 P1。
- **二次修正（用户反馈）**：发送消息后底部输入区必须和开始界面一致 → 移除 chat-bar 细条，会话开始后把整个 chat-box（含 +/模型/Knowledge/Deep Analysis chips 与 chat-note）移动到 `.chat-dock` 底部停靠位（ensureStarted/resetToWelcome 做 DOM 移动）；Send 圆钮在生成中原位变红色 Stop（boxSend type=button + .stop + 方块 SVG）；chips 会话中途因此也可用。
- **三次修正（用户反馈）**：对话态输入区①不显示长示例占位（停靠时换短文案 "Ask anything…"，回欢迎屏恢复）②高度调低（.chat-dock .chat-box padding 10/14/6、textarea min-height 26px、去阴影）③生成中不显示「正在生成中…」文字，仅 Send→Stop 图标切换（cpGenNote 已移除）④对话态隐藏输入区下方免责声明（ensureStarted 隐藏 chatNoteEl，回欢迎屏恢复；注意这与 FR-009「常驻」有出入，是用户明确的原型取舍）。
- **FR-016 演示已做**：`DISABLED_KBS` 登记表（Sanctions Screening=已停用、Industry Strategy Reports=权限回收）；挂载面板 kbmItems 过滤它们（FR-011）；planAnswer 开头检查 cur.mounts 命中即 plan.fail「知识库检索失败…无会话级快照·FR-016；移除该挂载后可重试」；seed4 会话（挂载 Sanctions Screening、含历史成功引用回答）演示"库停用后下一轮检索即失败"，用户移除挂载 chip 后 Retry 成功形成闭环。seed 机制改为版本感知（grc.chat.seed.ver=2），老用户 localStorage 已有会话也会补注 seed4。
- 模型下拉在页面底部被裁切：modelMenu 定位改为下方空间不足时向上弹出（同 placeSelectPanel 策略）+ maxHeight 260px 滚动。
- **FR-009c 已落地（v2 常驻化）**：detail 芯片改为工具行常驻——只要选中 vision 模型（gpt-4o/qwen3-vl-plus）即显示 `detail: auto ▾`，可在附图前预设；非 vision 模型隐藏（FR-009e 语义）。下拉含 auto/low/high + timeout（默认 120s，5~600 可覆盖）；visionAnswer 体现参数与 caller override。初版是"附图才显示"（渐进披露），用户要求可预设故改。
- **坑**：`.ro-banner{display:flex}` 等类规则与 UA `[hidden]{display:none}` 同优先级且后来者居上 → hidden 失效、空横幅占位（底部空条）。修复：全局加 `[hidden]{display:none!important}`。影响面：roBanner/cp-imgs/cp-mounts/cite-panel。该文件原有 `.view[hidden]{display:none!important}` 也是同一原因。
- 验证：HttpListener 临时服务器预览 200、JS lint 0 错误。本机无 node/python（python 为 WindowsApps 占位）；端口 8642 一度异常（脚本即启即退），8643 正常；execute_command 外层包装会吞掉 `$`，诊断脚本需写成 .ps1 文件执行；Get-Content 无 -Encoding 回写会被 Craft 拦截。预览服务器：Temp\grc-static-server.ps1（8643，后台运行中）。
