# 工作内容总结

**统计周期**：2026-09-01 至 2026-09-22  
**总结截至**：2026-09-23  
**资料范围**：根目录日期记录、`9.1~9.3对话`、两批会话导出记录及`周报`目录中的相关材料。

## 一、整体概览

本阶段工作围绕 GRC AI 平台的需求治理、知识库能力、Agent 生命周期和对话工作台展开，主要完成了以下四类工作：

1. 建立并完善 GRC Spec Hub 的需求事实源、Spec 目录、用户故事和角色权限边界。
2. 梳理知识库目录、构建流程、授权模型、向量化方案及安全护栏口径。
3. 形成测试与生产排期，明确核心模块上线批次，并识别排期冲突和准备窗口风险。
4. 围绕 Spec 001 对话工作台完成需求清单、跨 Spec 校核、实验台副本设计及前端交互原型补全。

## 二、分阶段工作回顾

### 1. 9 月 1 日至 9 月 4 日：Spec Hub 与需求治理基础建设

- 读取项目仓库和现有 Spec，完成所需 skills 的安装和工作环境准备。
- 梳理 GRC Spec Hub 的定位、目录结构及各 Spec 的职责边界，形成 000 至 009 feature spec 的基础目录。
- 根据 Spec 整理用户故事目录、角色权限边界和功能归属，明确需求、契约、实现和治理材料之间的关系。
- 梳理知识库目录结构，包括公共目录、个人目录、知识库生命周期、Owner/Deputy 等角色职责。
- 明确知识库构建链路为：Parse → Chunk → Enhancer → Embedding，并同步讨论文档、Chunk、向量化和检索之间的关系。
- 分析会议纪要对 Spec 的影响：已确定事项回写 Spec，未确定事项登记 watchlist，过程性讨论单独留痕。
- 形成 STR Monitoring 向量化设置及晚会方案与知识库 Spec 的对比结论。

### 2. 9 月 7 日至 9 月 11 日：排期、测试和上线计划

- 梳理平台功能点、测试用例和 Excel 排期，核对功能数量与模块归属。
- 设计 Quickly Guide 新手引导方案，并评估 Curate、Build 等页面的引导覆盖情况。
- 形成测试环境和生产环境的时间线展示方案，最终收敛为双轴信息在同一时间线中的呈现方式。
- 生产计划口径基本收敛为：Knowledge、Marketplace、Conversation 等核心模块于 10 月 16 日上线，其余模块于 10 月 30 日上线。
- 对排期中的日期和模块归属进行了多轮核对，发现部分 Excel、会议纪要和时间线存在不一致，需以后续正式排期为准。
- 识别出 Conversation 排期曾由 10 月 23 日调整为 10 月 10 日，但 10 月 10 日为周六，且距离 10 月 9 日 Gate 较近，仍需确认。

### 3. 9 月 14 日至 9 月 17 日：Conversation 功能清单与交付材料

- 评审 Quickly Guide 的聚光引导 IIFE 方案，提出锚点解耦、埋点、共享引导脚本、版本化配置、键盘及无障碍支持、角色切换和空态兜底等优化方向。
- 基于 Spec 001 梳理对话工作台功能清单，覆盖 Chat、基础输入、多模态图像、知识库挂载、Agent 透传、流式输出、护栏、回答渲染、历史会话和资产抽屉等模块。
- 对功能数量、优先级、一期范围和 Open Questions 进行核对，修正清单中“38 条”与实际 43 条 FR 不一致的问题。
- 在功能清单中增加“一期”列，并按照本期等于 P0、对应 M1 加 M2 的口径标记功能范围。
- 核对功能清单与仓库中的 `spec.md`、`plan.md`、`tasks.md` 及 watchlist，发现 Nexus Base64 PoC 已完成但 Spec 尚未回写等文档漂移。
- 将部分整理结果输出为 OneDrive 文档，供后续评审和需求确认使用。

### 4. 9 月 20 日：对话工作台原型落地与裁定收口

- 按 Spec 001 功能清单在 `marketplace - chat_agent.html` 中实现可交互原型，覆盖全部核心 FR 的演示路径。
- 保持原有页面布局，保留欢迎态和对话态两种形态，通过底部停靠的 Chat Box 承载输入、模型、知识库和深度分析等控件。
- 实现会话本地存储、90 天清理、500 条上限、软删除、重命名、示例会话及不可用资产状态演示。
- 实现图片输入限制、视觉模型判定、知识库挂载上限、检索结果归并、引用证据、原文查看及停用知识库重试闭环。
- 实现流式输出、停止生成、失败重试、输入和输出护栏演示，以及生成中的发送/停止状态切换。
- 实现 Agent 资产透传、我的资产抽屉、已下架和出站停用提示等交互。
- 补充视觉模型的 detail 参数预设、超时设置和参数覆盖逻辑，并修复隐藏元素显示规则导致的空横幅问题。
- 完成静态服务器预览和 JavaScript 语法检查，验证主要交互路径可运行。

同日完成了 Spec 001 待裁定项的整理：

- 滑动窗口参数和检测引擎继续挂起，保留 watchlist #1/#50。
- Nexus Base64 PoC 项关闭，并注明 T120 已通过，业务接入由 T117 至 T119 承担。
- 失败、护栏命中和停止生成后的落库规则按现有 FR-023、FR-025、FR-036 口径闭合。
- 上下文上限采用“最近 N 轮加 token 预算”的双上限方案，具体数值留待压测。
- cancel 路由采用一致性哈希为主、Redis 广播为兜底。
- 护栏最终按“网关统一执行”处理，Chat 只通过网关信号感知命中，命中轮次不回写。

### 5. 9 月 21 日至 9 月 22 日：跨 Spec 校核、实验台和界面细节补全

- 对 Spec 001 与 Knowledge、Build 等 Spec 进行只读冲突检查，确认知识库可挂载范围存在一处实质口径差异，其余检索、无快照、引用证据、凭据、门控和护栏规则基本一致。
- 按用户指令修订 Spec 001 副本，使知识库挂载范围对齐 Spec 005；修正 ADR-0015 悬空引用，并增加修订留痕。
- 将 Agent 透传会话重新定位为“实验台 Playground”，保留输入、输出、参数和原始报文的只读呈现，取消实验台记录的用户侧持久化。
- 新建 `spec-playground.md` 副本，重写 US4 相关场景，收窄历史会话范围，增加 Bench Turn 实体和实验台相关 FR。
- 明确实验台会话 ID 每次进入时生成，刷新、离开或切换后失效；补充复制和重新生成能力。
- 在 `marketplace - agent.html` 原型中完成实验台顶栏、参数面板、原始请求/响应报文、不可持久化、失效拒调和 Chat/实验台术语调整。
- 进一步声明实验台参数的唯一来源为 mgmt-service 的 `getMarketplaceAsset` 详情接口，并区分当前在架版本字段与即时生效字段。
- 在工作副本中以“纯增量”方式补充 Chat、流式中断、Agent 多轮展示、图片粘贴/拖拽等界面与交互细节，既有需求原文保持不变。
- 对补充内容进行排版统一，明确“缺口、候选、待确认”标记，避免尚未裁定的建议被误认为正式需求。

## 三、已形成的主要成果

### 1. 需求和治理成果

- 形成 GRC Spec Hub 的需求事实源和基本治理方法。
- 形成用户故事目录、角色权限边界、模块职责及跨 Spec 关联关系。
- 建立“Spec、契约、`memory/now`、watchlist 和人审门禁”相互配合的需求治理方式。
- 形成 Spec 001 对话工作台功能清单和一期范围标记，实际功能项按 43 条 FR 统计。

### 2. 知识库和权限成果

- 明确公共目录、个人目录、知识库生命周期和授权申请路径。
- 明确 Alice 为统一授权权威，审批权与使用权分离。
- 明确创建者不直接接触密钥，资产调用使用当前用户个人凭据。
- 明确知识库构建、切片、增强、向量化、检索和引用证据的基本口径。
- 明确知识库无权限用户只能查看 Information，并通过 Alice 申请权限。

### 3. 安全和运行时成果

- 护栏统一由平台网关执行，命中后拦截并纳入统一审计。
- 流式场景采用滑动窗口检测，非流式场景采用全文缓冲检测，具体参数和引擎仍待确认。
- 明确失败、护栏命中和取消生成时的落库规则，避免未完成或被拦截内容写入持久化会话。
- 梳理 PAT、个人凭据、平台默认凭据和 Agent 调用凭据的使用边界。

### 4. 原型和交互成果

- 完成 Chat 对话工作台主要交互原型，包括模型选择、图片输入、知识库挂载、引用证据、流式输出、护栏、历史会话和资产抽屉。
- 完成 Agent 实验台原型，包括参数只读面板、原始报文查看、复制、重新生成、失效提示和不持久化行为。
- 补充新手引导、流式中断、图片粘贴/拖拽、实验台多轮结果等界面细节候选方案。
- 对原型进行语法检查、静态预览和关键交互验证。

## 四、已确认的关键决策

1. Spec Hub 作为需求事实源；实现材料和讨论记录不能替代正式 Spec。
2. 知识库授权统一走 Alice，个人目录偏私有，公共目录按可见范围和角色控制。
3. 知识库链路采用 Parse、Chunk、Enhancer、Embedding 四阶段。
4. 护栏当前统一采用命中即拦截的运行时语义，不采用局部打码作为默认处理。
5. Chat 的护栏执行点归属网关，Agent service 只感知网关信号。
6. 实验台不是持久化 Chat，不承载完整的会话生命周期管理。
7. 实验台参数展示应与 mgmt-service 详情接口同源，不读取草稿、未发布版本或前端缓存。
8. 工作副本中的新增内容只能作为补充；标记为缺口、候选或待确认的内容尚不构成正式需求。

## 五、待确认事项与风险

### 1. 需求和技术待确认

- 滑动窗口的参数、检测引擎和安全阈值仍需业务与安全团队裁定。
- 实验台轮次是否纳入 PRD 第 9.8 节的 90 天脱敏留存范围仍未确定。
- 实验台原始请求/响应报文的脱敏边界仍需明确，尤其是鉴权头和平台内部字段。
- `MarketplaceAssetDetail.accessConfig` 中超时和重试字段的显式契约定义仍需确认。
- 订阅者是否能读取实验台参数展示所需字段，需要结合“按当前用户关系投影”的权限模型确认。
- ADR-0015 引用缺口仍需登记 watchlist 并由维护者处理。

### 2. 跨文档和跨模块风险

- Excel、会议纪要和时间线中存在日期及模块归属冲突，应指定正式排期来源并统一版本。
- 10 月 16 日同时承担测试收口和核心生产上线，独立生产准备窗口不足。
- Conversation 的 10 月 10 日排期落在周六，且距 Gate 时间较近，需重新确认。
- “Conversation 之前的全部功能”和“其余模块 10 月 30 日”的表述边界需要统一，当前按核心三模块解释。
- 原型已实现不代表正式 Spec、OpenAPI 契约和生产服务已完成，尤其是实验台透传接口仍存在契约影响。
- 实验台副本已处理的冲突暂未回合正本；Spec 004 的历史会话语义、Spec 002/005/007 的相关术语和旧 plan/tasks 仍需后续联动处理。

### 3. 原型与实现风险

- `create.html` 和 `marketplace - agent.html` 的 Agent 分支曾出现超时上限 180 秒、重试上限 5 次，与 Spec 003 的 120 秒和 3 次上限不一致，应登记并修正。
- 原型中的部分 Agent 参数为示例硬编码，真实环境必须改为从详情接口读取。
- Quickly Guide 的锚点解耦、埋点、共享配置、无障碍等优化建议尚未确认是否落地。

## 六、后续建议

1. 以 Spec Hub、正式排期表和已确认裁定为准，建立一份唯一的版本和日期基线，清理 Excel、会议纪要、周报之间的漂移。
2. 由业务、安全和合规共同关闭滑动窗口、安全阈值、实验台留存和报文脱敏等开放问题。
3. 评审 `spec-playground.md`，通过后再将必要内容回写 Spec 正本，并同步更新 plan、tasks、契约及跨 Spec 引用。
4. 补齐实验台透传接口契约，明确参数详情接口的字段、权限投影、版本口径和错误处理。
5. 将 Chat 和实验台原型中的关键交互整理为验收用例，重点覆盖护栏、取消、重试、停用资产、无权限知识库和不持久化行为。
6. 对原型中的硬编码示例数据、超时/重试边界和生产环境调用路径进行一次专项核查。
7. 继续维护 watchlist，并为每项待决策事项补充责任人、截止时间和关闭条件。

## 七、资料索引

- [2026-09-14.md](2026-09-14.md)：新手引导方案评审。
- [2026-09-17.md](2026-09-17.md)：Spec 001 功能清单、一期范围和编码处理经验。
- [2026-09-20.md](2026-09-20.md)：Chat 原型落地、功能清单核对和用户裁定。
- [2026-09-21.md](2026-09-21.md)：跨 Spec 校核、实验台副本、原型改造和参数来源声明。
- [2026-09-22.md](2026-09-22.md)：对话工作台界面与交互细节增量补充。
- [9.1~9.3对话](9.1~9.3对话)：Spec Hub、知识库、权限、向量化和早期周报材料。
- [会话批量导出-20260911134509](会话批量导出-20260911134509)：排期、Spec 整理和上线沟通材料。
- [会话批量导出-20260911135323](会话批量导出-20260911135323)：新手引导设计、页面引导和覆盖核查材料。
- [周报](周报)：阶段性对外汇总材料。

> 说明：本总结对重复记录进行了合并，以阶段成果和当前有效口径为主；原始会话、周报和工作副本仍是具体事实的追溯依据。